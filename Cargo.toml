[workspace]
members = [
    "modem-hal",
    "src-tauri",
]
resolver = "2"

[profile.release]
codegen-units = 1
lto = "thin"         # LTO=full 会移除 __TAURI_BUNDLE_TYPE_VAR_UNK 占位符
opt-level = 2
panic = "abort"
#strip = true        # 禁用 strip，Tauri 打包需要 patch 二进制
