Place Windows ADB runtime files in this directory before building a Windows package.

Required at runtime:
- adb.exe
- AdbWinApi.dll
- AdbWinUsbApi.dll

The desktop app resolves ADB from the packaged Tauri resource directory first.
